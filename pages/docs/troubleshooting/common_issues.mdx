# Troubleshooting: Common Issues

## Opening Vantage

If `npx vantage` doesn't open the application, make sure the `vantage_report.html` is in the `vantage` folder of the root directory.

## Running The Vantage Report
